# Visual layout checks

- Before publishing a visual change, inspect the supplied reference or screenshot at the affected breakpoint and compare it with its immediate surrounding layout.
- Keep repeated horizontal and vertical gaps intentionally aligned. When two gaps should match, define them from one shared value rather than tuning them separately.
- Check inherited widths, borders, and adjacent separators so a new element does not create unintended lines, uneven spacing, or misalignment.
